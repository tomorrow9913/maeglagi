# 프론트 전용 환경변수입니다. `cp .env.example .env.local` 후 사용하세요.
# 루트의 .env는 백엔드용이고 Next.js는 읽지 않습니다.

# FastAPI API 베이스 URL
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# true면 in-memory mock, false면 위 URL의 실 API를 호출합니다.
NEXT_PUBLIC_USE_MOCKS=true
